/** Role C — LiveKit token generation (M5) */
export async function createLiveKitToken(
  _roomName: string,
  _identity: string,
): Promise<{ token: string; url: string }> {
  throw new Error('LiveKit not configured — implement M5');
}
